import React from 'react';

const ToDoList = () => {
    return (
        <div>
            <h1>Todo List</h1>
            <button>Create Task</button>
        </div>
    );
};

export default ToDoList;