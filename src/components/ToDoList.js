import React from 'react';

const ToDoList = () => {
    return (
        <div>
            <h1>Todo List</h1>
            <button className = "btn btn-primary">Create Task</button>
        </div>
    );
};

export default ToDoList;